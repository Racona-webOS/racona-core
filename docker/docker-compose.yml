# =============================================================================
# Racona Core — Docker Compose konfiguráció
# =============================================================================
# Szolgáltatások:
#   - postgres:  PostgreSQL adatbázis
#   - db-init:   Adatbázis inicializálás (migráció + seed), egyszeri futás
#   - racona:    Racona webalkalmazás (SvelteKit + Express + Socket.IO)
#
# Indítási sorrend:
#   postgres (healthy) → db-init (completed) → racona
#
# Használat:
#   docker compose up -d          # Indítás háttérben
#   docker compose down           # Leállítás
#   docker compose logs -f        # Logok követése
#
# Környezeti változók:
#   Másold a gyökér .env.example fájlt .env-ként és töltsd ki az értékeket,
#   vagy állítsd be közvetlenül a környezeti változókat.
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # PostgreSQL adatbázis
  # ---------------------------------------------------------------------------
  postgres:
    build:
      context: postgres
      dockerfile: Dockerfile
    container_name: racona-postgres
    env_file:
      - ../.env.local
    volumes:
      - racona-data:/var/lib/postgresql
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    networks:
      - racona-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-racona} -d ${POSTGRES_DB:-racona}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # ---------------------------------------------------------------------------
  # Adatbázis inicializálás (migráció + seed) — egyszeri futás
  # ---------------------------------------------------------------------------
  db-init:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: racona-db-init
    command: >
      sh -c 'bun --filter @racona/database db:init ${RESET:+-- --reset}'
    env_file:
      - ../.env.local
    environment:
      - RESET=${RESET:-}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-racona}:${POSTGRES_PASSWORD:-racona123}@postgres:5432/${POSTGRES_DB:-racona}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - racona-network
    restart: 'no'

  # ---------------------------------------------------------------------------
  # Racona webalkalmazás
  # ---------------------------------------------------------------------------
  racona:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: racona-app
    ports:
      - '${ELYOS_PORT:-3000}:3000'
    env_file:
      # Csak a bootstrap credentials — minden más secret Infisical-ból érkezik Varlock-on keresztül
      - ../.env.local
    environment:
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
    depends_on:
      db-init:
        condition: service_completed_successfully
    networks:
      - racona-network
    volumes:
      - ../apps/web/uploads:/app/uploads
      - ../.env.local:/app/.env:ro
    restart: unless-stopped

# Megnevezett kötet a PostgreSQL adatok perzisztálásához
volumes:
  racona-data:
    driver: local

# Belső hálózat a szolgáltatások közötti kommunikációhoz
networks:
  racona-network:
    driver: bridge
